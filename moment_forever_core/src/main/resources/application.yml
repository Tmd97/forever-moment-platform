# ============================================
# SERVER CONFIGURATION
# ============================================
server:
  port: 8081
  forward-headers-strategy: framework
  servlet:
    context-path: /platform
    encoding:
      charset: UTF-8
      enabled: true
      force: true
  tomcat:
    connection-timeout: 30000
    max-connections: 200
    threads:
      max: 50
      min-spare: 5

# ============================================
# SPRING CONFIGURATION
# ============================================
spring:
  application:
    name: moment-forever-platform

  # ================= SERVICE DISCOVERY WITH CONSUL =================
  cloud:
    consul:
      host: ${CONSUL_HOST:localhost}
      port: 8500
      discovery:
        enabled: true
        register: true
        prefer-ip-address: true
        service-name: ${spring.application.name}
        health-check-path: /platform/actuator/health
        health-check-interval: 10s
        deregister-after: 1m
        instance-id: ${spring.application.name}:${random.value}

  # ================= DATABASE =================
  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST:localhost}:5432/moment_forever_db
    username: postgres
    password: groote
    driver-class-name: org.postgresql.Driver

  # ================= HIKARI =================
  hikari:
    pool-name: MomentForever-HikariPool
    connection-timeout: 30000
    maximum-pool-size: 20
    minimum-idle: 5
    idle-timeout: 600000
    max-lifetime: 1800000
    connection-test-query: SELECT 1
    auto-commit: false

  # ================= DATA CONFIG =================
  data:

    # ---------- MongoDB ----------
    mongodb:
      uri: mongodb://${MONGO_HOST:localhost}:27017
      database: moment_forever

    # ---------- Redis (FIXED) ----------
    redis:
      host: ${REDIS_HOST:localhost}
      port: 6379
      timeout: 2000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0

  # ================= JPA =================
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        jdbc:
          batch_size: 20
          order_inserts: true
          order_updates: true
        query:
          in_clause_parameter_padding: true
        id:
          new_generator_mappings: true

  # ================= QUARTZ CONFIG =================
  quartz:
    job-store-type: jdbc
    jdbc:
      initialize-schema: never
    properties:
      org:
        quartz:
          scheduler:
            instanceId: AUTO
            instanceName: clustered-scheduler
          jobStore:
            driverDelegateClass: org.quartz.impl.jdbcjobstore.PostgreSQLDelegate
            tablePrefix: QRTZ_
            isClustered: true
            clusterCheckinInterval: 20000
            misfireThreshold: 60000
          threadPool:
            class: org.quartz.simpl.SimpleThreadPool
            threadCount: 5
            threadPriority: 5

  # ================= FILE UPLOAD =================
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB
      file-size-threshold: 2KB

  # ================= JACKSON =================
  jackson:
    serialization:
      write-dates-as-timestamps: false
      indent-output: true
    deserialization:
      fail-on-unknown-properties: false
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: UTC
    default-property-inclusion: non_null

  # ================= KAFKA =================
  kafka:
    bootstrap-servers: ${SPRING_KAFKA_BOOTSTRAP_SERVERS:localhost:9092}

    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      acks: all

    consumer:
      group-id: platform-service-group
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: "*"

# ============================================
# KAFKA TOPICS (CUSTOM CONFIG)
# ============================================
kafka:
  topics:
    booking-requested: topic_booking_requested
    booking-confirmed: topic_booking_confirmed
    booking-failed: topic_booking_failed

# ============================================
# APPLICATION CUSTOM CONFIG
# ============================================
app:
  base-url: http://localhost:8080
  file:
    upload-dir: ./uploads/
    max-size: 5242880
    allowed-extensions: jpg,jpeg,png,gif,webp
    serve-path: /uploads/

  image:
    base-url: http://localhost:8081/api/platform
    public-base-url: /public/images
    admin-base-url: /admin/images
    cdn-base-url: https://cdn.momentforever.com/images
    cache-max-age: 31536000
    use-cdn: false
    thumbnail-size: 150x150
    medium-size: 600x400
    large-size: 1200x800

# ============================================
# SLUG CONFIG
# ============================================
slug:
  separator: "-"
  max-length: 100

# ============================================
# JWT SECURITY
# ============================================
jwt:
  secret: "your-256-bit-secret-key-must-be-at-least-32-characters-long-here"
  expiration: 86400000
  refresh-expiration: 604800000
  issuer: "cherishx.com"
  audience: "cherishx-web-app"

  cookie:
    name: "auth_token"
    secure: true
    http-only: true
    max-age: 86400

  cors:
    allowed-origins:
      - "http://localhost:3000"
      - "http://localhost:4200"
    allowed-methods:
      - "GET"
      - "POST"
      - "PUT"
      - "DELETE"
      - "OPTIONS"
    allowed-headers:
      - "Authorization"
      - "Content-Type"
      - "X-Requested-With"
    allow-credentials: true

  security:
    password:
      min-length: 8
      require-uppercase: true
      require-lowercase: true
      require-digits: true
      require-special-chars: true
      max-age-days: 90

  rate-limit:
    login:
      max-attempts: 5
      window-minutes: 15
    registration:
      max-per-hour: 10
      max-per-ip: 50

# ============================================
# ACTUATOR
# ============================================
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always
  health:
    discovery:
      enabled: true
  probes:
    enabled: true

# ============================================
# SWAGGER
# ============================================
springdoc:
  swagger-ui:
    path: /swagger-ui.html
    url: /v3/api-docs
  api-docs:
    path: /v3/api-docs

# ============================================
# OBJECT STORAGE (GRIDFS)
# ============================================
object:
  store:
    provider: gridfs
    gridfs-bucket: fs